import {readFile,writeFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {decideRelease} from './release-gate.mjs';
const root='reports/model-training-v1',read=p=>readFile(p,'utf8').then(JSON.parse);
const [manifest,b,c,control,training,delta,registration]=await Promise.all([read(root+'/manifest.json'),read(root+'/baseline.json'),read(root+'/candidate.json'),read(root+'/weights-control.json'),read('models/local-training/aurora-lora-v1/manifest.json'),read('models/local-training/aurora-lora-v1/weight-delta.json'),read('models/local-training/candidate-registration.json')]);
const decision=decideRelease(manifest,b,c,control);
const hashes={};for(const file of ['manifest.json','baseline.json','candidate.json','weights-control.json'])hashes[file]=createHash('sha256').update(await readFile(root+'/'+file)).digest('hex');
const summary={version:1,createdAt:new Date().toISOString(),model:manifest.candidate,baseModel:manifest.baseline,modelDigest:registration.model.digest,modelBytes:registration.model.size,training:{trainableParameters:training.trainableParameters,examples:200,developmentExamples:40,seconds:training.elapsedSeconds,peakAllocatedBytes:training.peakAllocatedBytes,adapterHash:training.adapterHash,changedTensors:delta.changedTensors,changedValues:delta.changedValues},decision,sourceHashes:hashes,scope:manifest.scope,limitations:['Amostra pequena: 12 tarefas HTML compostas com 2 sementes.','Componentes semelhantes aparecem no treino; as respostas destes desafios não aparecem.','Não mede precisão geral, projetos grandes ou funcionamento em computadores modestos.','Tempo medido localmente; energia é apenas simulação, não leitura da tomada.']};
await writeFile(root+'/summary.json',JSON.stringify(summary,null,2));
const f=n=>n.toLocaleString('pt-BR',{maximumFractionDigits:1}),s=summary;
await writeFile(root+'/dashboard.html',`<!doctype html><html lang="pt-BR"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Aurora · Treinamento local</title><style>body{margin:0;background:#0c1424;color:#e8edf8;font:16px system-ui}main{max-width:1050px;margin:auto;padding:40px 24px}h1{font-size:36px;margin-bottom:8px}p{line-height:1.6;color:#b5c3da}.badge{color:#ffc98d}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.card,section{background:#142139;border:1px solid #263953;border-radius:14px;padding:20px;margin:20px 0}.big{display:block;font-size:30px;color:#6bdde9}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:12px;border-bottom:1px solid #2b3b53}input{background:#0c1424;color:white;border:1px solid #637590;padding:8px;width:90px}li{margin:10px 0}.good{color:#76e1b0}.bad{color:#ffb98e}@media(max-width:650px){.grid{grid-template-columns:1fr}main{padding:20px}table{font-size:13px}}</style><main><p>AURORA / LABORATÓRIO LOCAL</p><h1>Pesos treinados. Resultado verificado.</h1><p class="badge">${decision.passed?'Aprovado nos critérios experimentais':'Experimental · não aprovado como padrão'}</p><p>LoRA sobre Qwen2.5-Coder 1.5B. O treinamento altera parâmetros; o arquivo local final continua com aproximadamente 986 MB.</p><div class="grid"><div class="card">Parâmetros ajustados<strong class="big">${f(training.trainableParameters/1e6)} milhões</strong></div><div class="card">Treinamento medido<strong class="big">${f(training.elapsedSeconds)} s</strong></div><div class="card">Exemplos de treino<strong class="big">200</strong>+ 40 de desenvolvimento</div></div><section><h2>O que mudou nos desafios reservados</h2><table><tr><th>Métrica</th><th>Original</th><th>Treinado</th></tr>${[['Execuções aprovadas',b.passed+'/24',c.passed+'/24'],['Tokens totais',f(b.tokens),f(c.tokens)],['Chamadas',b.calls,c.calls],['Tempo total',f(b.elapsedMs/1000)+' s',f(c.elapsedMs/1000)+' s'],['Tokens por aprovação',f(b.tokens/b.passed),f(c.tokens/c.passed)],['Retenção simples',b.retentionPassed+'/4',c.retentionPassed+'/4']].map(r=>'<tr>'+r.map(v=>'<td>'+v+'</td>').join('')+'</tr>').join('')}</table><p>Diferença: ${f(decision.gainPoints)} pontos percentuais. Menos tokens não representam melhoria se a aprovação cair.</p></section><section><h2>Por área</h2><table><tr><th>Área</th><th>Original</th><th>Treinado</th></tr>${decision.domains.map(d=>'<tr><td>'+d.domain+'</td><td>'+d.baseline+'/'+d.total+'</td><td>'+d.candidate+'/'+d.total+'</td></tr>').join('')}</table></section><section><h2>Decisão de uso</h2><ul>${decision.checks.map(x=>'<li class="'+(x.passed?'good':'bad')+'">'+(x.passed?'✓ ':'✗ ')+x.name+'</li>').join('')}</ul><p>${decision.reason}</p><p>Controle de conversão (primeira tentativa): ${decision.control.map(x=>x.model+': '+x.passed+'/'+x.total).join('; ')}.</p></section><section><h2>Energia: hipótese editável</h2><label>Tarifa R$/kWh <input id="tariff" type="number" min="0" step="0.1" value="1"></label> <label>Potência estimada W <input id="watts" type="number" min="0" step="10" value="300"></label><p id="energy"></p><p>Simulação de treinamento e dos dois grupos de avaliação. Não inclui instalação, conversão, controle adicional, equipamento ou trabalho humano. Não houve API paga de geração neste experimento.</p></section><section><h2>Como interpretar</h2><ul>${s.limitations.map(x=>'<li>'+x+'</li>').join('')}</ul><p>Pesos efetivamente alterados em ${delta.changedTensors} tensores. Artefatos, saídas completas, critérios prévios e hashes ficam ao lado deste dashboard.</p></section></main><script>const seconds=${training.elapsedSeconds+(b.elapsedMs+c.elapsedMs)/1000};function update(){const cost=seconds/3600*Math.max(0,Number(document.getElementById('watts').value)||0)/1000*Math.max(0,Number(document.getElementById('tariff').value)||0);document.getElementById('energy').textContent='Energia simulada: '+cost.toLocaleString('pt-BR',{style:'currency',currency:'BRL',minimumFractionDigits:4});}document.getElementById('tariff').oninput=update;document.getElementById('watts').oninput=update;update();</script></html>`);
console.log(JSON.stringify(summary));
