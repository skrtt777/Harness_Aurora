import {readFile,writeFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {decideRelease} from './release-gate.mjs';
const root='reports/model-training-v2',read=p=>readFile(p,'utf8').then(JSON.parse),hash=x=>createHash('sha256').update(x).digest('hex');
const [manifest,b,c,control,selection,training,dataset]=await Promise.all([read(root+'/manifest.json'),read(root+'/final/baseline.json'),read(root+'/final/candidate.json'),read(root+'/final/control.json'),read(root+'/selection.json'),read('models/local-training/aurora-lora-v2/manifest.json'),read('models/local-training/dataset-v2/manifest.json')]);
const epoch=Number(selection.selected.slice(1));const exported=await read(`models/local-training/aurora-lora-v2/export-e${epoch}/registration.json`);
if(c.modelDigest!==selection.modelDigest||exported.model.digest!==selection.modelDigest)throw Error('Selected weights differ from evaluated weights');
if(training.trainHash!==dataset.trainHash||dataset.trainHash!==manifest.dataset.trainHash)throw Error('Training provenance mismatch');
for(const [f,h] of Object.entries(manifest.sourceHashes))if(hash(await readFile(root+'/source/'+f))!==h)throw Error('Archived source mismatch '+f);
const canonicalComparison={summary:[{model:control.model,passed:control.firstPassed,total:control.total},{model:c.model,passed:c.firstPassed,total:c.total}],records:[...control.records,...c.records]};
const gateDecision=decideRelease(manifest,b,c,canonicalComparison);
const stats=r=>({passed:r.passed,total:r.total,tokens:r.tokens,elapsedMs:r.elapsedMs,calls:r.calls,retentionPassed:r.retentionPassed});
// A rejected/incomplete usage measurement still gets an honest report. This
// enrichment is display-only; activation rechecks the unmodified gateDecision.
const decision=gateDecision.baseline?gateDecision:{...gateDecision,baseline:stats(b),candidate:stats(c),domains:[...new Set(manifest.tasks.map(t=>t.domain))].map(domain=>({domain,baseline:b.records.filter(r=>r.domain===domain&&r.passed).length,candidate:c.records.filter(r=>r.domain===domain&&r.passed).length,total:manifest.tasks.filter(t=>t.domain===domain).length*manifest.seeds.length})),gainPoints:100*(c.passed/c.total-b.passed/b.total),tokenRatio:c.tokens/b.tokens,control:canonicalComparison.summary};
// Paired bootstrap by task, keeping both seeds of each task together.
let rng=20260921;const random=()=>{rng^=rng<<13;rng^=rng>>>17;rng^=rng<<5;return(rng>>>0)/4294967296;};
const differences=manifest.tasks.map(t=>c.records.filter(r=>r.id===t.id&&r.passed).length/manifest.seeds.length-b.records.filter(r=>r.id===t.id&&r.passed).length/manifest.seeds.length),samples=[];
for(let n=0;n<5000;n++){let sum=0;for(let j=0;j<differences.length;j++)sum+=differences[Math.floor(random()*differences.length)];samples.push(sum/differences.length*100);}samples.sort((a,b)=>a-b);
const interval=[samples[125],samples[4874]];
for(const [file,value] of [['baseline.json',b],['candidate.json',c],['weights-control.json',canonicalComparison]])await writeFile(root+'/'+file,JSON.stringify(value,null,2));
const sourceHashes={};for(const file of ['manifest.json','baseline.json','candidate.json','weights-control.json','selection.json'])sourceHashes[file]=hash(await readFile(root+'/'+file));
const summary={version:2,createdAt:new Date().toISOString(),model:manifest.candidate,baseModel:manifest.baseline,modelDigest:selection.modelDigest,modelBytes:selection.modelBytes,training:{trainableParameters:training.trainableParameters,examples:dataset.train,developmentExamples:dataset.dev,seconds:training.elapsedSeconds,peakAllocatedBytes:training.peakAllocatedBytes,adapterHash:exported.adapterHash,mergedHash:exported.mergedHash,ggufHash:exported.ggufHash,changedTensors:0,browserFamilies:dataset.browserFamilies.length,algorithmFamilies:dataset.algorithmFamilies},selection,decision,gateDecision,sourceHashes,scope:manifest.scope,confidenceIntervalPoints:interval,limitations:['12 tarefas novas com duas sementes: amostra pequena, não precisão geral.','As tarefas de desenvolvimento já foram observadas; seus resultados não são o teste final.','Componentes relacionados aparecem no treino; referências dos desafios finais foram excluídas.','Resultados da v1 e da v2 usam desafios finais diferentes e não devem ser comparados diretamente.','Energia é uma simulação editável. Hardware usado: RTX 4090; desempenho em máquinas menores não foi medido.']};
// Actual tensor-change proof is attached separately; do not invent a count.
const delta=await read(`models/local-training/aurora-lora-v2/export-e${epoch}/weight-delta.json`);summary.training.changedTensors=delta.changedTensors;
const missingUsage=[b,c].some(r=>r.records.some(x=>x.estimatedTokens>0));
const controlMissingUsage=control.records.some(x=>x.estimatedTokens>0);
if(missingUsage)summary.limitations.unshift('Uma ou mais chamadas abortaram sem contagem completa de tokens. Totais exibidos são somente tokens medidos; não há conclusão de economia e a promoção foi bloqueada.');
if(controlMissingUsage)summary.limitations.unshift('O controle final teve uma correção interrompida, mantida como falha: 2.502 tokens estimados além dos 46.260 medidos. Não há conclusão exata de economia frente ao controle. Original instalado e candidata têm contagem completa.');
await writeFile(root+'/summary.json',JSON.stringify(summary,null,2));
const f=n=>Number.isFinite(n)?n.toLocaleString('pt-BR',{maximumFractionDigits:1}):'—',savings=100*(1-c.tokens/b.tokens),approvalCost=x=>x.passed?x.tokens/x.passed:NaN;
const devControl=await read(root+'/dev/control.json');
const controlSavings=100*(1-c.tokens/control.tokens);
const comparisonRows=[['Execuções aprovadas',b.passed+'/24',control.passed+'/24',c.passed+'/24'],['Primeira tentativa',b.firstPassed+'/24',control.firstPassed+'/24',c.firstPassed+'/24'],[missingUsage?'Tokens medidos (incompletos)':'Tokens totais',f(b.tokens),f(control.tokens)+(controlMissingUsage?' + estimativa':''),f(c.tokens)],['Chamadas',b.calls,control.calls,c.calls],['Tempo total',f(b.elapsedMs/1000)+' s',f(control.elapsedMs/1000)+' s',f(c.elapsedMs/1000)+' s'],['Tokens por aprovação',f(approvalCost(b)),controlMissingUsage?'incompleto':f(approvalCost(control)),f(approvalCost(c))],['Retenção simples',b.retentionPassed+'/4',control.retentionPassed+'/4',c.retentionPassed+'/4']];
const outcome='O treinamento não demonstrou ganho de qualidade: '+control.passed+'/24 na base equivalente e '+c.passed+'/24 na candidata. A retenção caiu de '+control.retentionPassed+'/4 para '+c.retentionPassed+'/4. Tokens '+(missingUsage||controlMissingUsage?'sem comparação conclusiva frente à base equivalente':f(controlSavings)+'% menores frente à base equivalente')+'.';
await writeFile(root+'/dashboard.html',`<!doctype html><html lang="pt-BR"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Aurora · Treinamento v2</title><style>body{margin:0;background:#0c1424;color:#e8edf8;font:16px system-ui}main{max-width:1050px;margin:auto;padding:36px 24px}h1{font-size:36px;margin:12px 0}p{line-height:1.6;color:#becbe0}.badge{color:#ffc98d}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.card,section{background:#142139;border:1px solid #263953;border-radius:14px;padding:22px;margin:18px 0}.big{display:block;font-size:30px;color:#6bdde9}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:12px;border-bottom:1px solid #2b3b53}input{background:#0c1424;color:white;border:1px solid #637590;padding:8px;width:95px}li{margin:10px 0}.good{color:#76e1b0}.bad{color:#ffb98e}a{color:#6bdde9}@media(max-width:650px){.grid{grid-template-columns:1fr}main{padding:16px}.table{overflow:auto}th,td{padding:8px;font-size:13px}}</style><main><p>AURORA / LABORATÓRIO LOCAL · V2</p><h1>Mais diversidade. Avaliação separada.</h1><p class="badge">${decision.passed?'Critérios experimentais atingidos':'Experimental · não aprovado como padrão'}</p><p>Mesma base de 1,5 bilhão de parâmetros. Formato de conversa alinhado ao Harness, menos repetição, ajuste mais conservador e escolha por funcionamento real.</p><div class="grid"><div class="card">Exemplos de treino<strong class="big">${dataset.train}</strong>${dataset.browserFamilies.length} famílias de navegador + ${dataset.algorithmFamilies} de funções</div><div class="card">Parâmetros ajustados<strong class="big">${f(training.trainableParameters/1e6)} milhões</strong>taxa de ajuste 5 vezes menor</div><div class="card">Treinamento medido<strong class="big">${f(training.elapsedSeconds)} s</strong>duas versões intermediárias</div></div><section><h2>Desafios finais: o efeito do treinamento</h2><p><strong>${outcome}</strong></p><p>A base equivalente usa a mesma revisão, conversão, template e quantização da candidata.</p><div class="table"><table><tr><th>Métrica</th><th>Original instalado</th><th>Base equivalente sem treino</th><th>Aurora v2</th></tr>${comparisonRows.map(r=>'<tr>'+r.map(v=>'<td>'+v+'</td>').join('')+'</tr>').join('')}</table></div><p>Comparação secundária com o original instalado — diferença: <strong>${f(decision.gainPoints)} pontos percentuais</strong>. Economia total de tokens: ${missingUsage?'não conclusiva':f(savings)+'%'}. Intervalo descritivo de 95% por reamostragem das tarefas: ${f(interval[0])} a ${f(interval[1])} pontos.</p><p>Falhas também entram no custo. A v1 usou desafios diferentes; a comparação válida nesta tabela é entre modelos submetidos aos mesmos desafios v2.</p></section><section><h2>Por área</h2><table><tr><th>Área</th><th>Original</th><th>Treinado</th></tr>${decision.domains.map(d=>'<tr><td>'+d.domain+'</td><td>'+d.baseline+'/'+d.total+'</td><td>'+d.candidate+'/'+d.total+'</td></tr>').join('')}</table></section><section><h2>Escolha antes do teste final</h2><p>24 tarefas antigas foram usadas como desenvolvimento. Selecionada ${selection.selected==='e1'?'época 1':'época 2'}, sem consultar os desafios finais.</p><table><tr><th>Versão</th><th>Desenvolvimento</th><th>Retenção</th><th>Tokens</th></tr><tr><td>Base sem treino</td><td>${devControl.passed}/24</td><td>${devControl.retentionPassed}/4</td><td>${f(devControl.tokens)}</td></tr>${selection.development.map(d=>'<tr><td>'+d.arm+'</td><td>'+d.passed+'/'+d.total+'</td><td>'+d.retentionPassed+'/4</td><td>'+f(d.tokens)+'</td></tr>').join('')}</table><p>Esses resultados orientam a escolha e não são apresentados como ganho em tarefas inéditas. Na época 2, uma correção foi interrompida pelo Ollama por repetição excessiva e permaneceu reprovada, sem nova tentativa. Tokens de desenvolvimento incluem 2.347 tokens estimados para essa interrupção; no teste final, o controle também teve uma correção interrompida, mantida como falha, com 2.502 tokens estimados adicionais. Original instalado e candidata têm contagem integral medida.</p></section><section><h2>Decisão de uso</h2><ul>${decision.checks.map(x=>'<li class="'+(x.passed?'good':'bad')+'">'+(x.passed?'✓ ':'✗ ')+x.name+'</li>').join('')}</ul><p>${decision.reason}</p><p>Controle com a mesma conversão, primeira tentativa: ${control.firstPassed}/24 sem treino; ${c.firstPassed}/24 com treino.</p></section><section><h2>Energia estimada</h2><label>R$/kWh <input id="tariff" type="number" min="0" step="0.1" value="1"></label> <label>Potência W <input id="watts" type="number" min="0" step="10" value="300"></label><p id="energy"></p><p>Simulação do treinamento e das avaliações finais original/treinado. Não inclui preparação, desenvolvimento, exportação, controle adicional ou equipamento. Sem API paga de geração.</p></section><section><h2>Limites da evidência</h2><ul>${summary.limitations.map(x=>'<li>'+x+'</li>').join('')}</ul><p><a href="summary.json">Métricas e hashes</a> · <a href="manifest.json">Critérios fixados antes do treino</a></p></section></main><script>const seconds=${training.elapsedSeconds+(b.elapsedMs+c.elapsedMs)/1000};function update(){const cost=seconds/3600*Math.max(0,Number(document.getElementById('watts').value)||0)/1000*Math.max(0,Number(document.getElementById('tariff').value)||0);document.getElementById('energy').textContent='Energia simulada: '+cost.toLocaleString('pt-BR',{style:'currency',currency:'BRL',minimumFractionDigits:4});}document.getElementById('tariff').oninput=update;document.getElementById('watts').oninput=update;update();</script></html>`);
await writeFile('docs/MODELO_LOCAL_V2.md',`# Aurora local v2\n\n${decision.passed?'A candidata atingiu os critérios experimentais.':'A candidata não atingiu todos os critérios e permanece experimental.'}\n\n## Mudanças nesta rodada\n\n- Mesma base oficial Qwen2.5-Coder-1.5B-Instruct e mesma revisão da v1. A base original foi preservada.\n- ${dataset.train} exemplos: ${dataset.categories.generation} gerações HTML, ${dataset.categories.repair} correções JSON, ${dataset.categories.javascript} funções JavaScript e ${dataset.categories.retention} exemplos de retenção/formato.\n- ${dataset.browserFamilies.length} famílias de comportamento de navegador, com ${dataset.browserCases} referências corretas e defeitos reproduzidos; ${dataset.algorithmFamilies} famílias de funções com ${dataset.algorithmCasesChecked} verificações executadas. Variações de IDs e valores não são habilidades independentes.\n- Treino no formato ChatML realmente usado pelo Ollama no Harness, sem mensagem de sistema implícita adicional. A v1 usava o template do Hugging Face que inseria essa mensagem.\n- LoRA rank 8, ${training.trainableParameters.toLocaleString('pt-BR')} parâmetros ajustáveis, taxa 0,00002 (cinco vezes menor), duas épocas. Tempo: ${f(training.elapsedSeconds)} s; pico alocado pelo PyTorch: ${f(training.peakAllocatedBytes/1e9)} GB. Não representa consumo total de VRAM ou requisito mínimo da máquina.\n- Duas versões intermediárias avaliadas em 24 tarefas de desenvolvimento já observadas. Seleção: ${selection.selected}; ordem dos critérios: aprovação funcional, retenção, tokens e época anterior. As referências dos novos desafios finais não entram no treinamento nem na seleção.\n\n## Resultado final em desafios novos\n\n${outcome}\n\n| Métrica | Original instalado | Base equivalente sem treino | Aurora v2 |\n|---|---:|---:|---:|\n${comparisonRows.map(r=>'| '+r.join(' | ')+' |').join('\n')}\n\nComparação secundária com o original instalado: diferença de aprovação: ${f(decision.gainPoints)} pontos percentuais. Intervalo descritivo de 95% por bootstrap pareado por tarefa: [${f(interval[0])}, ${f(interval[1])}] pontos. Economia total de tokens: ${missingUsage?'não conclusiva':f(savings)+'%'}. Valores menores não significam melhoria quando a aprovação também cai.\n\n${decision.domains.map(d=>'- '+d.domain+': '+d.baseline+'/'+d.total+' na base e '+d.candidate+'/'+d.total+' na candidata.').join('\n')}\n\nControle de conversão com a mesma revisão, template e quantização: ${control.firstPassed}/24 sem treino e ${c.firstPassed}/24 com treino na primeira tentativa.\n\n## Critérios de promoção\n\n${decision.checks.map(x=>'- '+(x.passed?'Passou: ':'Não passou: ')+x.name+'.').join('\n')}\n\nOs limiares anteriores foram preservados: aprovação mínima de 70%, ganho de 15 pontos percentuais, nenhuma regressão por área, retenção mínima de 3/4 sem regressão, tokens até 120% da base e ganho frente ao controle de conversão. O modelo não é promovido apenas por ter nome próprio ou pesos diferentes.\n\n## Evidências e reprodução\n\n- Dashboard: http://127.0.0.1:8794. Arquivos: reports/model-training-v2/.\n- Modelo Ollama: ${manifest.candidate}. Digest: ${selection.modelDigest}.\n- Adaptador selecionado: models/local-training/aurora-lora-v2/epoch-${epoch}/adapter_model.safetensors.\n- Pesos incorporados e GGUF: models/local-training/aurora-lora-v2/export-e${epoch}/. Alterações reais observadas em ${delta.changedTensors} tensores, registradas em weight-delta.json.\n- scripts/training/build-dataset-v2.mjs gera e valida os dados; train-lora-v2.py treina; export-v2.py incorpora os adaptadores e exporta; evaluate-v2.mjs avalia; select-v2.mjs fixa a candidata antes do teste final.\n- Manifesto original e código arquivado, decisão de seleção, hashes, logs, todas as respostas e validações são preservados. Os diretórios de treino recusam sobrescrita. Para nova experiência, criar IDs novos e reservar novos desafios.\n- Houve uma correção de escape na expressão regular da referência do contador de palavras, detectada na pré-verificação antes de qualquer resposta final de modelo. Requisitos, contratos e limiares ficaram idênticos. Manifesto inicial e alteração estão preservados em manifest-initial.json e amendments.json.\n- No desenvolvimento da época 2, o Ollama interrompeu uma correção por repetição excessiva. A falha foi preservada e contabilizada, sem reexecução; 2.347 tokens foram estimados e cobrados no orçamento. A seleção foi corrigida para aceitar essa falha registrada, sem alterar a ordem por aprovações. O controle final também teve uma correção interrompida, mantida como falha, com 2.502 tokens estimados adicionais. Original instalado e candidata têm uso integralmente medido.\n- O teste final desta rodada já foi observado. Não reutilizá-lo para escolher outro checkpoint e continuar chamando-o de teste independente.\n\n## Limites\n\n${summary.limitations.map(x=>'- '+x).join('\n')}\n\nTreinamento, preparação e seleção mudaram juntos nesta rodada. A comparação mostra o efeito da receita completa; não isola qual mudança foi responsável por cada diferença. A tarifa de R$ 1,00/kWh e potência de 300 W no dashboard são hipóteses editáveis, não medição real. Não houve API paga de geração.\n`);
console.log(JSON.stringify(summary));
